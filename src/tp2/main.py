import os
from tp2.utils.functions import Functions
from tp2.utils.config import logger

if __name__ == "__main__":
    logger.info(f"Key openai value : {os.getenv('OPENAI_KEY')}")

    def main():
        logger.info("Hello World")
